<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Print Report Example</title>
    <!-- Use Tailwind CSS for easy styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Add a custom style for print media to hide elements not needed in the printed output */
        @@media print {
            body > *:not(#report-container) {
                display: none;
            }
            #report-container {
                display: block;
                width: 100%;
                margin: 0;
            }
        }
    </style>
</head>
<body class="bg-gray-100 p-8 font-sans antialiased text-gray-800">

    <div class="max-w-4xl mx-auto bg-white rounded-xl shadow-lg p-8 space-y-6">

        <!-- Header Section -->
        <div class="flex flex-col sm:flex-row justify-between items-center pb-4 border-b border-gray-200">
            <h1 class="text-3xl font-bold text-gray-800">Payment Report</h1>
            <!-- Print button, calls the JavaScript function on click -->
            <button onclick="printReport()" class="mt-4 sm:mt-0 px-6 py-2 bg-blue-600 text-white font-semibold rounded-lg shadow-md hover:bg-blue-700 transition duration-300 ease-in-out">
                Print Report
            </button>
        </div>

        <!-- The container holding the content you want to print -->
        <div id="report-container" class="space-y-4">
            <h2 class="text-2xl font-semibold text-gray-700 mb-4">Payment Details</h2>

            <!-- The table that will be printed -->
            <table id="report-table" class="min-w-full divide-y divide-gray-200 rounded-lg overflow-hidden">
                <thead class="bg-blue-500 text-white">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider">Order No.</th>
                        <th class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider">Order Date</th>
                        <th class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider">Member ID</th>
                        <th class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider">Total Value</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    <!-- Sample data rows -->
                    <tr>
                        <td class="px-6 py-4 whitespace-nowrap">12345</td>
                        <td class="px-6 py-4 whitespace-nowrap">2023-01-15</td>
                        <td class="px-6 py-4 whitespace-nowrap">M9876</td>
                        <td class="px-6 py-4 whitespace-nowrap">$150.00</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 whitespace-nowrap">12346</td>
                        <td class="px-6 py-4 whitespace-nowrap">2023-01-16</td>
                        <td class="px-6 py-4 whitespace-nowrap">M5432</td>
                        <td class="px-6 py-4 whitespace-nowrap">$75.50</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 whitespace-nowrap">12347</td>
                        <td class="px-6 py-4 whitespace-nowrap">2023-01-17</td>
                        <td class="px-6 py-4 whitespace-nowrap">M1098</td>
                        <td class="px-6 py-4 whitespace-nowrap">$220.75</td>
                    </tr>
                </tbody>
            </table>
        </div>
        <!-- End of report-container -->

    </div>

    <!--
        The JavaScript is placed here, just before the closing </body> tag.
        This is a best practice to ensure the HTML elements exist
        and are fully loaded before the script tries to access them.
        This prevents "ReferenceError: ... is not defined" errors.
    -->
    <script>
        /**
         * The `printReport` function gets a specific HTML element and prints its content.
         * It creates a new window, writes a minimal HTML document to it, and then
         * prints the content of the target element.
         */
        function printReport() {
            // Get the HTML content of the element you want to print using its ID.
            const contentToPrint = document.getElementById('report-container');

            // Check if the element exists.
            if (!contentToPrint) {
                console.error("The element with ID 'report-container' was not found.");
                // You could display a user-friendly message here, e.g., using a custom modal.
                return;
            }

            // Open a new browser window. The arguments define its size and position.
            const printWindow = window.open('', '_blank', 'height=600,width=800');

            // Write the basic HTML structure for the new window.
            // This ensures the printed document has a proper head, title, and CSS.
            printWindow.document.write('<!DOCTYPE html><html><head><title>Payment Report</title>');
            // Link to a CSS file for styling in the printed document.
            // You can use the same CSS framework or custom print styles.
            printWindow.document.write('<link rel="stylesheet" href="https://cdn.tailwindcss.com" />');
            printWindow.document.write('</head><body>');
            
            // Write the HTML content of the specific element to the new window.
            // Using .outerHTML gets the element itself and its contents.
            printWindow.document.write(contentToPrint.outerHTML);
            
            // Close the HTML document. This is crucial for the content to be rendered correctly.
            printWindow.document.close();

            // Trigger the print dialog for the new window.
            printWindow.print();

            // Optional: Close the new window automatically after printing.
            // This is commented out because it can sometimes be a poor user experience.
            // printWindow.close();
        }
    </script>

</body>
</html>
